---
export interface Props {
  title: string;
}

const { title } = Astro.props;
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
---

<!DOCTYPE html>
<html lang="en" class="bg-gray-950">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <noscript>
      <style>
        :root {
          --noscript: initial !important;
        }
      </style>
    </noscript>
  </head>
  <!-- {satoshi.variable} {spaceMono.variable} -->
  <body
    class={`
		  font-mono min-h-screen bg-[#17181d] text-white`}
  >
    <div class="max-w-[900px] !mx-auto px-2 flex flex-col min-h-screen">
      <Navbar />
      <div class="flex-1">
        <slot />
      </div>
      <Footer />
    </div>
  </body>
</html>
<style is:global>
  @font-face {
    font-family: "SpaceMono";
    src: url("/fonts/space-mono/SpaceMono-Regular.ttf") format("truetype");
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  :root {
    --noscript: false;
    --accent: 124, 58, 237;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      #da62c4 30%,
      white 60%
    );
  }
  html {
    font-family: "SpaceMono", system-ui, sans-serif !important;
    background-color: #f6f6f6;
  }

  .js-enhanced-component {
    display: var(--noscript, none);
  }
  /* 
	.glowing-button {
		background-image: linear-gradient(
			90.92deg,
			#f4c15f -25.43%,
			#ffb677 127.94%
		);
		background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	.glowing-button:hover {
		background-image: linear-gradient(
			90.92deg,
			#f4c15f -25.43%,
			#ffb677 127.94%
		);
	} */
</style>
